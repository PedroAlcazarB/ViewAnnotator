services:
  # Servicio MongoDB
  mongo:
    image: mongo:6.0
    container_name: viewannotator_mongo_prod
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_INITDB_DATABASE:-viewannotator}
    volumes:
      - ./mongodb/data:/data/db
      - ./mongodb/logs:/var/log/mongodb
      - ./scripts/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    networks:
      - viewannotator_network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Servicio Backend
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: viewannotator_backend_prod
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - FLASK_ENV=${FLASK_ENV:-production}
      - FLASK_APP=${FLASK_APP:-app.py}
      - MONGO_URI=${MONGO_URI:-mongodb://mongo:27017/}
      - SECRET_KEY=${SECRET_KEY:?SECRET_KEY no definido en .env}
    volumes:
      - ./backend/datasets:/app/datasets
      - ./backend/ai_models:/app/ai_models
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - viewannotator_network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Servicio Frontend (único punto de entrada)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: viewannotator_frontend_prod
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-5050}:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - viewannotator_network

# Red para comunicación entre servicios
networks:
  viewannotator_network:
    driver: bridge 